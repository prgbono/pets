import { Pet } from '@/types';
declare const PetCard: ({ height, id, kind, length, name, number_of_lives, photo_url, weight }: Pet) => import("react/jsx-runtime").JSX.Element;
export default PetCard;
