export declare const calculatePetHealth: (kind: string, weight: number, height: number, length: number, number_of_lives: number) => "unhealthy" | "very healthy" | "healthy";
export declare const getTextColorClass: (health: string) => "text-red-600" | "text-green-600";
